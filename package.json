{
  "name": "@onivoro/server-process",
  "version": "0.1.0",
  "scripts": {
    "test": "jest",
    "build": "tsc -p tsconfig.json",
    "release": "npm version minor && cd dist && npm publish --scope public"
  },
  "devDependencies": {
    "@types/jest": "^26.0.14",
    "@types/node": "^14.11.2",
    "jest": "^26.4.2",
    "ts-jest": "^26.4.1",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "rxjs": "^6.6.3"
  }
}
